from distutils.core import setup

install_requires = [
    'enum34',
    'ndg-httpsclient',
    'requests',
]

setup(name='tuleap-rest-api-client',
      version='1.0',
      description='Tuleap REST API client',
      author='xxx',
      author_email='xxx@example.com',
      url='https://www.example.com',
      install_requires=install_requires
      )
